import React from 'react';

const Header = props => {
    return (
        <header className="top">
            <h1>
                Cotizador de Seguro de auto
            </h1>
        </header>
    )
};

export default Header;